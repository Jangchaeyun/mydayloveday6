const cloud_name = "dmxnml9p1";
const upload_preset = "mydayloveday6";

export const uploadToCloudinary = async (pics, fileType) => {
  if (pics && fileType) {
    
  } else {
    console.log("error........");
  }
};
